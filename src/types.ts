export interface School {
  cityCode: string; // 縣市代碼
  cityName: string; // 縣市名稱
  schoolCode: string; // 學校代碼
  schoolName: string; // 學校名稱
  levelName: string; // 等級名稱
  dayNightName: string; // 日夜別名稱
  groupCode: string; // 群別代碼
  groupName: string; // 群別名稱
  departmentName: string; // 科系名稱
}
